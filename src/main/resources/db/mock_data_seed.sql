-- =============================================================================
-- Use the main mock script instead:
--   src/main/resources/data.sql
--
-- That file is adapted from the original data.sql content to the current
-- Hibernate schema (departments, users, rooms, bookings, notifications,
-- permissions, role_permissions).
-- =============================================================================

SELECT 'Run src/main/resources/data.sql instead' AS hint;
