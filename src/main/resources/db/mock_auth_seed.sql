-- =============================================================================
-- Auth + full mock data are now combined in:
--   src/main/resources/data.sql
--
-- Run that single script after the app has created tables.
-- =============================================================================

SELECT 'Run src/main/resources/data.sql instead' AS hint;
